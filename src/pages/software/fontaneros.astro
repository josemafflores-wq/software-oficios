---
import Layout from '../../layouts/Layout.astro';
import softwares from '../../softwares.json';

const info = {
  id: 'fontaneros',
  nombre: 'Fontaneros',
  emoji: '🔧',
  titulo: 'Software para Fontaneros y App de Partes de Trabajo',
  desc: 'El mejor software y App móvil para gestionar avisos de fontanería, controlar el stock de la furgoneta y facturar sin internet.',
  filtro: ['fontaneros', 'fontanería', 'desatascos', 'pocería', 'calefacción'],
  opinionKey: 'opinion_experto_fontanero',
  guiaTitulo: 'Guía 2026: Cómo elegir App y Programa para Fontanería',
  guiaTexto: 'La fontanería moderna requiere movilidad total. No basta con un programa de oficina; necesitas una <strong>App de partes de trabajo</strong> que permita geolocalizar al técnico y facturar in-situ.',
  bloques: [
    { icon: '📱', t: 'App de Partes', d: 'Crea partes con fotos del "antes y después", recoge la firma del cliente en la pantalla y ciérralos al momento.' },
    { icon: '📦', t: 'Stock en Furgoneta', d: 'Controla el stock de grifería y cobre. Descuenta el material del almacén móvil automáticamente.' },
    { icon: '🧾', t: 'Facturación Verifactu', d: 'Evita sanciones. A partir de 2026, tus facturas deben llevar un código QR y Huella digital.' }
  ],
  faqs: [
    { q: "¿Es legal facturar con Excel tras la Ley Verifactu?", a: "No. A partir de 2026/27, el uso de Excel será ilegal ya que no garantiza la inalterabilidad. Necesitas un software homologado con código QR." },
    { q: "¿Cómo controlo el material de cada operario?", a: "Los programas recomendados permiten gestionar 'almacenes móviles'. El stock se descuenta de la furgoneta al cerrar el parte de trabajo." },
    { q: "¿Qué pasa en sótanos sin cobertura?", a: "Las mejores Apps son 'Offline First'. Permiten trabajar sin conexión y sincronizan los datos automáticamente al recuperar señal." }
  ]
};

const vipList = ['stel-order', 'taclia'];
const fontanerosSoft = softwares
  .filter(s => s.target_gremios.some(g => g.toLowerCase().includes('fontane')))
  .sort((a, b) => {
      const vipA = vipList.indexOf(a.id);
      const vipB = vipList.indexOf(b.id);
      if (vipA !== -1 && vipB !== -1) return vipA - vipB;
      if (vipA !== -1) return -1;
      if (vipB !== -1) return 1;
      return (b.legal_spain?.verifactu_ready ? 1 : 0) - (a.legal_spain?.verifactu_ready ? 1 : 0);
  });

const topTable = fontanerosSoft.slice(0, 5);

const schema = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": info.faqs.map(faq => ({
    "@type": "Question",
    "name": faq.q,
    "acceptedAnswer": { "@type": "Answer", "text": faq.a }
  }))
};
---

<Layout title="Mejor Software para Fontaneros 2026 | Apps Offline">
  <script type="application/ld+json" set:html={JSON.stringify(schema)} />

  <header class="bg-blue-900 text-white pt-24 pb-20 px-6 relative overflow-hidden text-center md:text-left">
    <div class="absolute top-0 right-0 opacity-10 text-[200px]">🔧</div>
    <div class="max-w-4xl mx-auto relative z-10">
      <span class="bg-blue-800 text-blue-200 text-xs font-bold px-3 py-1 rounded-full uppercase mb-4 inline-block tracking-widest">Especial Fontanería</span>
      <h1 class="text-4xl md:text-6xl font-black mb-6 leading-tight">Software para <br/><span class="text-blue-400">Fontaneros y Calefactores</span></h1>
      <p class="text-xl text-blue-100 max-w-2xl leading-relaxed">{info.desc}</p>
    </div>
  </header>

  <main class="max-w-5xl mx-auto px-6 -mt-16 relative z-20 pb-20">
    {/* VEREDICTO RÁPIDO */}
    <div class="bg-white rounded-2xl shadow-xl border border-slate-200 overflow-hidden mb-16">
        <div class="bg-slate-900 p-4 text-center"><h2 class="text-white font-bold uppercase tracking-widest text-sm">🏆 Veredicto Rápido</h2></div>
        <div class="grid md:grid-cols-3 divide-y md:divide-y-0 md:divide-x divide-slate-100">
            <div class="p-6 text-center">
                <div class="text-3xl mb-2">🥇</div>
                <h3 class="font-black text-slate-800">STEL Order</h3>
                <p class="text-xs text-slate-500 mb-4">La mejor App Offline para garajes.</p>
                <a href="/analisis/stel-order?sector=fontaneros" class="text-blue-600 font-bold text-sm">Ver Análisis →</a>
            </div>
            <div class="p-6 text-center">
                <div class="text-3xl mb-2">🚀</div>
                <h3 class="font-black text-slate-800">Taclia</h3>
                <p class="text-xs text-slate-500 mb-4">Gestión sencilla y gratuita.</p>
                <a href="/analisis/taclia?sector=fontaneros" class="text-green-600 font-bold text-sm">Ver Análisis →</a>
            </div>
            <div class="p-6 text-center">
                <div class="text-3xl mb-2">⚙️</div>
                <h3 class="font-black text-slate-800">Fixner</h3>
                <p class="text-xs text-slate-500 mb-4">Ideal para mantenimientos.</p>
                <a href="/analisis/fixner?sector=fontaneros" class="text-purple-600 font-bold text-sm">Ver Análisis →</a>
            </div>
        </div>
    </div>

    {/* LISTADO TARJETAS */}
    <div class="space-y-12 mb-20">
        {fontanerosSoft.map((soft) => (
            <div class="bg-white rounded-2xl shadow-lg border border-slate-200 overflow-hidden">
                <div class="p-6 md:p-10 flex flex-col md:flex-row justify-between gap-6">
                    <div class="flex items-start gap-6">
                        <div class="w-20 h-20 bg-white rounded-xl flex items-center justify-center border border-slate-200 p-2 shrink-0">
                            {soft.logo_url ? <img src={soft.logo_url} alt={soft.name} class="w-full h-full object-contain" /> : "📱"}
                        </div>
                        <div>
                            <h3 class="text-2xl font-black text-slate-800">{soft.name}</h3>
                            <p class="text-slate-600 mt-4 leading-relaxed">{soft.opinion_experto_fontanero || soft.short_description}</p>
                        </div>
                    </div>
                    <div class="flex flex-col gap-3 min-w-[140px]">
                        <span class="text-3xl font-black text-center text-slate-900">{soft.pricing?.start_price === 0 ? 'GRATIS' : `${soft.pricing.start_price}€`}</span>
                        <a href={soft.url_afiliado || "#"} target="_blank" class="bg-blue-600 text-white font-black py-3 rounded-xl text-center shadow-lg">PRUEBA GRATIS</a>
                    </div>
                </div>
            </div>
        ))}
    </div>

    {/* 🚨 GUÍA RECUPERADA + ICONOS + FAQs 🚨 */}
    <section class="bg-white rounded-2xl shadow-lg border border-slate-200 overflow-hidden p-8 md:p-12">
        <h2 class="text-3xl font-black text-slate-800 mb-6">{info.guiaTitulo}</h2>
        <p class="text-lg leading-relaxed text-slate-600 mb-10" set:html={info.guiaTexto}></p>

        {/* 3 BLOQUES DE ICONOS */}
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-12 border-y border-slate-100 py-10">
            {info.bloques.map(b => (
                <div class="text-center md:text-left">
                    <div class="text-4xl mb-4">{b.icon}</div>
                    <h4 class="font-bold text-slate-900 mb-2">{b.t}</h4>
                    <p class="text-sm text-slate-600">{b.d}</p>
                </div>
            ))}
        </div>

        <h3 class="text-2xl font-black text-slate-800 mb-8">Dudas frecuentes sobre Fontanería</h3>
        <div class="space-y-4">
            {info.faqs.map(faq => (
                <details class="group bg-slate-50 border border-slate-200 rounded-xl transition-all">
                    <summary class="p-6 font-bold text-slate-800 cursor-pointer list-none flex justify-between items-center group-open:bg-blue-50 group-open:rounded-t-xl">
                        {faq.q} <span class="text-blue-600 group-open:rotate-180 transition-transform">▼</span>
                    </summary>
                    <div class="p-6 text-slate-600 bg-white rounded-b-xl border-t border-slate-100 text-sm">{faq.a}</div>
                </details>
            ))}
        </div>
    </section>
  </main>
</Layout>