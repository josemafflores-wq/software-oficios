---
// src/pages/software/carpinteros.astro
import softwares from '../../softwares.json';
import '../../global.css';
import Header from '../../components/header.astro';

// 1. FILTRO CARPINTEROS
const especialistas = softwares.filter(s => 
    s.target_gremios && s.target_gremios.some(g => {
        const gremio = g.toLowerCase();
        return ['carpinteros', 'carpintería', 'madera', 'aluminio', 'pvc', 'metalistería', 'reformas', 'autónomos'].some(term => gremio.includes(term));
    })
);
const topTable = especialistas.slice(0, 5);

// 2. SCHEMA UPDATE (FAQs específicas de Carpintería)
const schema = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": [
    { "@type": "Question", "name": "¿El software permite despiece de materiales?", "acceptedAnswer": { "@type": "Answer", "text": "Sí, es vital para calcular costes. Permite introducir las medidas (alto x ancho) y el material (aluminio, madera, vidrio) para calcular el escandallo exacto." } },
    { "@type": "Question", "name": "¿Puedo incluir bocetos en el presupuesto?", "acceptedAnswer": { "@type": "Answer", "text": "Sí. Los mejores programas para carpintería permiten adjuntar planos, fotos o bocetos 3D directamente en el presupuesto para que el cliente firme el diseño." } }
  ]
};
---

<html lang="es">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Software para Carpintería y Aluminio 2025: Presupuestos y Medidas</title>
    <meta name="description" content="Comparativa de software para carpinterías de madera, aluminio y PVC. Gestión de despieces, presupuestos con medidas y Ley Verifactu." />
    <script type="application/ld+json" set:html={JSON.stringify(schema)} />
</head>
<body class="bg-slate-50 text-slate-900 font-sans">

    <Header />

    <header class="bg-amber-900 text-white py-16 px-6">
        <div class="max-w-4xl mx-auto">
            <h1 class="text-4xl md:text-5xl font-black mb-4 italic">
                Software para Carpintería: <span class="text-amber-400">Tus medidas bajo control.</span>
            </h1>
            <p class="text-lg text-amber-100 opacity-90">
                Herramientas para talleres de madera y metálica. Presupuestos por medidas, control de fabricación y diseño visual.
            </p>
        </div>
    </header>

    <section class="max-w-6xl mx-auto px-6 -mt-10 relative z-20 mb-16">
        <div class="bg-white rounded-2xl shadow-2xl border border-slate-200 overflow-hidden">
            <div class="bg-slate-900 p-4 flex justify-between items-center">
                <h2 class="text-white font-bold text-sm uppercase tracking-widest">Mejores Programas Taller</h2>
                <span class="bg-green-600 text-white text-[10px] px-2 py-1 rounded font-bold animate-pulse">VERIFACTU READY</span>
            </div>
            <div class="overflow-x-auto">
                <table class="w-full text-left">
                    <thead>
                        <tr class="bg-slate-50 border-b border-slate-200 text-xs font-bold uppercase text-slate-500">
                            <th class="p-4">Software</th>
                            <th class="p-4">Especialidad</th>
                            <th class="p-4">Precio Base</th>
                            <th class="p-4 text-center">App</th>
                            <th class="p-4"></th>
                        </tr>
                    </thead>
                    <tbody class="text-sm">
                        {topTable.map((s) => (
                            <tr class="border-b border-slate-100 hover:bg-amber-50/50 transition-colors">
                                <td class="p-4 font-bold text-slate-800 flex items-center gap-2">
                                    {s.logo_url && <img src={s.logo_url} class="w-6 h-6 object-contain" />}
                                    {s.name}
                                </td>
                                <td class="p-4 text-slate-600 font-medium">{s.target_gremios ? s.target_gremios[0] : 'General'}</td>
                                <td class="p-4 font-black text-blue-700">{s.pricing?.start_price ? `${s.pricing.start_price}€` : '-'}</td>
                                <td class="p-4 text-center">{s.tabla_tecnica?.app_movil ? '📱' : '-'}</td>
                                <td class="p-4"><a href={s.url_afiliado} target="_blank" class="bg-amber-800 text-white text-[10px] font-black px-3 py-2 rounded uppercase hover:bg-amber-900 transition">Ver Web</a></td>
                            </tr>
                        ))}
                    </tbody>
                </table>
            </div>
        </div>
    </section>

    <main class="max-w-5xl mx-auto py-12 px-6">
        <div class="space-y-10">
            {especialistas.map((soft) => (
                <div class="bg-white rounded-2xl shadow-lg border border-slate-200 overflow-hidden hover:shadow-xl transition-shadow">
                    <div class="p-6 md:p-10">
                        <div class="flex flex-col md:flex-row md:items-center justify-between gap-6 mb-8 relative">
                            <div class="flex items-center gap-6">
                                <div class="w-24 h-24 bg-white rounded-xl flex items-center justify-center border border-slate-200 shadow-sm p-3 shrink-0 relative group">
                                    {soft.logo_url ? <img src={soft.logo_url} alt={soft.name} class="w-full h-full object-contain" /> : <span class="text-3xl font-black text-slate-300">{soft.name[0]}</span>}
                                    {soft.legal_spain?.verifactu_ready && (
                                        <div class="absolute -top-3 -right-3 bg-green-600 text-white text-[10px] font-black px-2 py-1 rounded shadow-md border-2 border-white z-10">VERIFACTU</div>
                                    )}
                                </div>
                                <div>
                                    <h3 class="text-3xl font-black text-slate-800"><a href={`/analisis/${soft.id}?sector=carpinteros`} class="hover:text-amber-700">{soft.name}</a></h3>
                                    <div class="flex flex-col gap-1 mt-2">
                                        {soft.legal_spain?.verifactu_ready && <span class="flex items-center text-green-700 font-bold text-xs bg-green-50 px-2 py-1 rounded border border-green-200">HOMOLOGADO VERIFACTU</span>}
                                    </div>
                                </div>
                            </div>
                            <div class="flex flex-col items-end">
                                <span class="text-xs font-bold text-slate-400 uppercase tracking-widest">Cuota mensual</span>
                                <span class="text-3xl font-black text-slate-900">{soft.pricing?.start_price ? `${soft.pricing.start_price}€` : 'Consultar'}</span>
                                {soft.pricing?.has_free_trial && <span class="text-blue-600 text-xs font-bold mt-1 bg-blue-50 px-2 py-0.5 rounded">Prueba Gratis {soft.pricing.trial_days} días</span>}
                            </div>
                        </div>

                        <div class="grid grid-cols-1 md:grid-cols-2 gap-10">
                            <div class="space-y-6">
                                <div class="prose prose-slate">
                                    <h4 class="text-xl font-bold text-slate-800">¿Por qué para carpintería?</h4>
                                    <p class="text-slate-600 leading-relaxed text-sm">
                                        {soft.short_description}
                                    </p>
                                </div>
                                <div class="flex flex-wrap gap-2">
                                    {soft.features && soft.features.slice(0, 4).map(f => (
                                        <span class="bg-amber-50 text-amber-900 text-xs font-bold px-3 py-1 rounded-md border border-amber-200 uppercase tracking-tight">{f}</span>
                                    ))}
                                </div>
                            </div>
                            <div class="flex flex-col justify-center bg-slate-50 p-6 rounded-2xl border border-slate-100">
                                <h4 class="font-bold text-slate-800 mb-4 text-center italic text-sm">
                                    "La herramienta para presupuestar sin errores"
                                </h4>
                                <div class="space-y-3">
                                    <a href={soft.url_afiliado || "#"} target="_blank" class="block w-full text-center bg-amber-700 hover:bg-amber-800 text-white font-black py-4 rounded-xl text-lg shadow-xl shadow-amber-200 transition-transform active:scale-95">
                                        VER WEB DE {soft.name.toUpperCase()}
                                    </a>
                                    <a href={`/analisis/${soft.id}?sector=carpinteros`} class="block w-full text-center bg-white border-2 border-slate-200 text-slate-700 font-bold py-3 rounded-xl hover:bg-slate-50 transition-colors text-sm">
                                        Leer análisis
                                    </a>
                                </div>
                                <p class="text-[10px] text-center text-slate-400 mt-4 uppercase font-bold tracking-widest">Enlace oficial verificado · 2025</p>
                            </div>
                        </div>
                    </div>
                </div>
            ))}
        </div>

        <div class="mt-20 bg-white rounded-2xl shadow-lg border border-slate-200 overflow-hidden p-8 md:p-12">
            <div class="prose prose-slate max-w-none">
                <h2 class="text-3xl font-black text-slate-800 mb-6">Guía 2025: Software para Carpintería de Madera y Aluminio</h2>
                <p class="text-lg leading-relaxed text-slate-600">
                    Gestionar un taller no es fácil. Tienes que calcular despieces, gestionar retales, controlar el stock de herrajes y, además, presentar presupuestos atractivos para que el cliente no se vaya a la competencia.
                </p>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-3 gap-8 not-prose my-10 border-y border-slate-100 py-10">
                <div class="p-6 bg-blue-50 rounded-2xl border border-blue-100">
                    <div class="text-4xl mb-4">📏</div>
                    <h4 class="font-bold text-blue-900 mb-3 text-lg">1. Presupuestos por Medidas</h4>
                    <p class="text-sm text-blue-800 leading-relaxed">
                        Olvídate de la calculadora. El software debe permitirte meter "Alto x Ancho" y calcular automáticamente los metros cuadrados de vidrio o los metros lineales de perfilería.
                    </p>
                </div>
                <div class="p-6 bg-amber-50 rounded-2xl border border-amber-100">
                    <div class="text-4xl mb-4">🖼️</div>
                    <h4 class="font-bold text-amber-900 mb-3 text-lg">2. Diseño Visual 3D</h4>
                    <p class="text-sm text-amber-900 leading-relaxed">
                        Vende más enseñando el resultado. Herramientas como Houzz Pro te permiten mostrarle al cliente cómo quedará el armario empotrado o la ventana antes de fabricarla.
                    </p>
                </div>
                <div class="p-6 bg-green-50 rounded-2xl border border-green-100">
                    <div class="text-4xl mb-4">⚖️</div>
                    <h4 class="font-bold text-green-900 mb-3 text-lg">3. Normativa Verifactu</h4>
                    <p class="text-sm text-green-800 leading-relaxed">
                        Como cualquier autónomo, tu taller debe emitir facturas legales. El software garantiza que cumplas con la Ley Antifraude y evita sanciones de Hacienda.
                    </p>
                </div>
            </div>

            <div class="prose prose-slate max-w-none">
                <h3 class="text-2xl font-bold text-slate-800">Preguntas Frecuentes en el Taller</h3>
                <details class="group bg-slate-50 p-5 rounded-xl cursor-pointer mb-4 border border-slate-100">
                    <summary class="font-bold text-slate-800 flex justify-between items-center list-none text-lg">
                        ¿Puedo gestionar la instalación en casa del cliente?
                        <span class="text-blue-600 group-open:rotate-180 transition-transform">▼</span>
                    </summary>
                    <p class="text-slate-600 mt-3 leading-relaxed">
                        Sí. Con la App móvil de programas como STEL Order, tus montadores pueden llevar el albarán digital, marcar la obra como terminada y que el cliente firme la recepción en el móvil.
                    </p>
                </details>
                 <p class="text-sm italic text-slate-500 mt-8">
                    * Todos los programas analizados permiten la emisión de facturas simplificadas y completas cumpliendo la normativa española.
                </p>
            </div>
        </div>
    </main>
</body>
</html>