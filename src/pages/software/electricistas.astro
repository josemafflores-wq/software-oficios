---
// src/pages/software/electricistas.astro
// 🚨 PÁGINA PILAR DEFINITIVA - Restauración Total: Diseño PRO + Tabla Técnica + FAQs + Schema
import Layout from '../../layouts/Layout.astro';
import softwares from '../../softwares.json';

const info = {
  id: 'electricistas',
  nombre: 'Electricistas',
  emoji: '⚡',
  titulo: 'Software para Instaladores Electricistas y SAT',
  desc: 'Gestiona boletines, mantenimientos preventivos y actualiza el precio del cable al día. Cumple el REBT y la Ley Verifactu 2026.',
  guiaTitulo: 'Digitalización para el Instalador Eléctrico 2026',
  guiaTexto: 'El sector eléctrico tiene particularidades críticas: precios de materiales volátiles (cobre) y entornos SAT complejos. Necesitas automatizar las revisiones para asegurar ingresos recurrentes.',
  bloques: [
    { icon: '🔄', t: 'Mantenimientos', d: 'Automatiza revisiones de cuadros, extintores o plantas fotovoltaicas con avisos recurrentes.' },
    { icon: '🔌', t: 'Tarifas Proveedor', d: 'Importa el Excel de tu proveedor y actualiza el precio del cobre masivamente.' },
    { icon: '🔦', t: 'Trabajo Offline', d: 'Ideal para trabajar en cuartos de contadores o naves industriales sin cobertura.' }
  ],
  faqs: [
    { q: "¿Cómo actualizo precios si sube el cobre?", a: "Los programas especializados permiten importar las tarifas de tus proveedores y aplicar márgenes automáticos al instante a todo tu catálogo." },
    { q: "¿Me ayuda con los boletines eléctricos?", a: "Permiten adjuntar los certificados técnicos a la ficha del cliente y recoger la firma digital legal desde el móvil del instalador." },
    { q: "¿Es obligatorio Verifactu para autónomos?", a: "Sí. A partir de 2026 es obligatorio garantizar la trazabilidad de facturas y generar el código QR legal en cada ticket." }
  ]
};

// 🔥 SEO: DATOS ESTRUCTURADOS AUTOMÁTICOS (Schema FAQPage)
const schema = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": info.faqs.map(faq => ({
    "@type": "Question",
    "name": faq.q,
    "acceptedAnswer": {
      "@type": "Answer",
      "text": faq.a
    }
  }))
};

const vipList = ['stel-order', 'taclia'];
const electricistasSoft = softwares
  .filter(s => s.target_gremios.some(g => {
      const gremio = g.toLowerCase();
      return gremio.includes('electric') || gremio.includes('instalador') || gremio.includes('sat');
  }))
  .sort((a, b) => {
      const vipA = vipList.indexOf(a.id);
      const vipB = vipList.indexOf(b.id);
      if (vipA !== -1 && vipB !== -1) return vipA - vipB;
      if (vipA !== -1) return -1;
      if (vipB !== -1) return 1;
      return (b.legal_spain?.verifactu_ready ? 1 : 0) - (a.legal_spain?.verifactu_ready ? 1 : 0);
  });

const topTable = electricistasSoft.slice(0, 5);
---

<Layout title="Mejor Software para Electricistas 2026 | Tarifas y REBT">
  
  {/* 🔥 INYECCIÓN DE SCHEMA EN EL HEAD (Vital para Rich Snippets) */}
  <script type="application/ld+json" set:html={JSON.stringify(schema)} />
  
  {/* HERO HEADER PRO */}
  <header class="bg-blue-900 text-white pt-24 pb-20 px-6 relative overflow-hidden">
    <div class="absolute top-0 right-0 opacity-10 text-[200px] pointer-events-none italic font-black">⚡</div>
    <div class="max-w-4xl mx-auto relative z-10 text-center md:text-left">
      <span class="bg-yellow-500 text-yellow-900 text-[10px] font-black px-3 py-1 rounded-full uppercase tracking-widest mb-4 inline-block shadow-lg">Especial Instaladores Eléctricos</span>
      <h1 class="text-4xl md:text-6xl font-black mb-6 leading-tight tracking-tighter">Software para <br/><span class="text-blue-400">Electricistas e Instaladores</span></h1>
      <p class="text-xl text-blue-100 max-w-2xl leading-relaxed font-medium">{info.desc}</p>
    </div>
  </header>

  <main class="max-w-5xl mx-auto px-6 -mt-16 relative z-20 pb-20">
    
    {/* 1. CHEAT SHEET (TOP 3 RECOMENDADO) */}
    <div class="bg-white rounded-2xl shadow-2xl border border-slate-200 overflow-hidden mb-16">
        <div class="bg-slate-900 p-4 text-center"><h2 class="text-white font-bold uppercase tracking-widest text-xs">🏆 Recomendación de Experto</h2></div>
        <div class="grid md:grid-cols-3 divide-y md:divide-y-0 md:divide-x divide-slate-100">
            <div class="p-8 text-center hover:bg-blue-50/50 transition-colors">
                <div class="text-4xl mb-3">🥇</div>
                <h3 class="font-black text-slate-800 text-lg">STEL Order</h3>
                <span class="text-[10px] font-bold text-blue-600 uppercase tracking-tighter">Líder en Tarifas</span>
                <p class="text-xs text-slate-500 mt-3 italic line-clamp-2">Imbatible para catálogos masivos y actualización de cable.</p>
                <div class="mt-4"><a href="/analisis/stel-order/?sector=electricistas" class="text-blue-600 font-bold text-sm hover:underline">Ver Análisis →</a></div>
            </div>
            <div class="p-8 text-center hover:bg-green-50/50 transition-colors">
                <div class="text-4xl mb-3">🔌</div>
                <h3 class="font-black text-slate-800 text-lg">Taclia</h3>
                <span class="text-[10px] font-bold text-green-600 uppercase tracking-tighter">Fácil y Gratis</span>
                <p class="text-xs text-slate-500 mt-3 italic line-clamp-2">Perfecto para autónomos que solo necesitan facturar rápido.</p>
                <div class="mt-4"><a href="/analisis/taclia/?sector=electricistas" class="text-green-600 font-bold text-sm hover:underline">Ver Análisis →</a></div>
            </div>
            <div class="p-8 text-center hover:bg-purple-50/50 transition-colors">
                <div class="text-4xl mb-3">⚙️</div>
                <h3 class="font-black text-slate-800 text-lg">Fixner</h3>
                <span class="text-[10px] font-bold text-purple-600 uppercase tracking-tighter">Rey del SAT</span>
                <p class="text-xs text-slate-500 mt-3 italic line-clamp-2">El mejor para mantenimientos de cuadros y plantas.</p>
                <div class="mt-4"><a href="/analisis/fixner/?sector=electricistas" class="text-purple-600 font-bold text-sm hover:underline">Ver Análisis →</a></div>
            </div>
        </div>
    </div>

    {/* 2. TABLA COMPARATIVA TÉCNICA */}
    <div class="bg-white rounded-2xl shadow-xl border border-slate-200 overflow-hidden mb-16">
        <div class="bg-slate-100 p-4 border-b border-slate-200 text-center md:text-left">
            <h3 class="font-bold text-slate-700 uppercase text-xs tracking-wider">Comparativa Técnica Eléctrica</h3>
        </div>
        <div class="overflow-x-auto">
            <table class="w-full text-left text-sm">
                <thead>
                    <tr class="bg-slate-50 border-b border-slate-200 text-xs font-bold uppercase text-slate-500 text-[10px]">
                        <th class="p-4">Software</th>
                        <th class="p-4">Precio Base</th>
                        <th class="p-4 text-center">App Móvil</th>
                        <th class="p-4 text-center">Verifactu</th>
                        <th class="p-4"></th>
                    </tr>
                </thead>
                <tbody class="text-slate-700">
                    {topTable.map((s) => (
                        <tr class="border-b border-slate-50 hover:bg-slate-50 transition-colors font-medium">
                            <td class="p-4 font-bold flex items-center gap-3">
                                {s.logo_url ? <img src={s.logo_url} class="w-8 h-8 object-contain" alt="" /> : "📱"} {s.name}
                            </td>
                            <td class="p-4 font-black text-blue-700">
                                {s.pricing?.start_price === 0 ? "GRATIS" : `${s.pricing.start_price}€`}
                            </td>
                            <td class="p-4 text-center text-lg">{s.tabla_tecnica?.app_movil ? '✅' : '❌'}</td>
                            <td class="p-4 text-center text-lg">{s.legal_spain?.verifactu_ready ? '✅' : '❌'}</td>
                            <td class="p-4 text-right">
                                <a href={s.url_afiliado} target="_blank" class="bg-blue-600 text-white text-[10px] font-black px-4 py-2 rounded uppercase shadow-sm">Ver Web</a>
                            </td>
                        </tr>
                    ))}
                </tbody>
            </table>
        </div>
    </div>

    {/* 3. LISTADO DE TARJETAS PRO */}
    <div class="space-y-16 mb-24">
        {electricistasSoft.map((soft) => (
            <div class="bg-white rounded-3xl shadow-xl border border-slate-200 overflow-hidden hover:shadow-2xl transition-all duration-300">
                <div class="p-6 md:p-12">
                    <div class="flex flex-col md:flex-row md:items-start justify-between gap-8 mb-10">
                        <div class="flex items-start gap-6">
                            <div class="w-24 h-24 bg-white rounded-2xl flex items-center justify-center border border-slate-100 shadow-sm p-3 shrink-0">
                                {soft.logo_url ? <img src={soft.logo_url} alt={soft.name} class="w-full h-full object-contain" /> : <span class="text-4xl font-black text-slate-200">📱</span>}
                            </div>
                            <div>
                                <h3 class="text-3xl font-black text-slate-800">
                                    <a href={`/analisis/${soft.id}/?sector=electricistas`} class="hover:text-blue-700 underline decoration-blue-200 decoration-8 underline-offset-8 transition-all">{soft.name}</a>
                                </h3>
                                <div class="flex flex-wrap gap-2 mt-5">
                                    {soft.legal_spain?.verifactu_ready && <span class="text-green-700 font-black text-[10px] uppercase bg-green-50 px-2 py-1 rounded-md border border-green-200 tracking-widest">✅ Verifactu Ready</span>}
                                    <span class="text-blue-700 font-black text-[10px] uppercase bg-blue-50 px-2 py-1 rounded-md border border-blue-200 tracking-widest italic font-bold">Análisis Experto</span>
                                </div>
                            </div>
                        </div>
                        <div class="bg-slate-50 p-4 rounded-2xl text-center min-w-[160px] border border-slate-100">
                            <span class="block text-[10px] font-bold text-slate-400 uppercase mb-1">Desde</span>
                            <span class="text-4xl font-black text-slate-900">{soft.pricing?.start_price === 0 ? 'GRATIS' : `${soft.pricing.start_price}€`}</span>
                            {soft.pricing?.has_free_trial && <div class="text-blue-600 text-[10px] font-black mt-2 uppercase tracking-tighter italic">Prueba Gratuita</div>}
                        </div>
                    </div>

                    <div class="grid grid-cols-1 md:grid-cols-3 gap-12 items-center border-t border-slate-50 pt-10">
                        <div class="md:col-span-2">
                            <div class="flex items-center gap-2 mb-4">
                                <span class="w-8 h-1 bg-blue-600 rounded-full"></span>
                                <h4 class="text-xs font-black text-slate-400 uppercase tracking-widest italic tracking-tighter">¿Por qué para {info.nombre}?</h4>
                            </div>
                            <p class="text-slate-600 text-lg leading-relaxed mb-8 italic">"{soft.opinion_experto_electricista || soft.short_description}"</p>
                            <div class="flex flex-wrap gap-3">
                                {soft.features?.slice(0, 5).map(f => <span class="bg-slate-100 text-slate-600 text-[11px] font-bold px-3 py-1.5 rounded-lg border border-slate-200 uppercase tracking-tight">{f}</span>)}
                            </div>
                        </div>
                        <div class="flex flex-col gap-4">
                            <a href={soft.url_afiliado || "#"} target="_blank" rel="nofollow noopener" class="bg-blue-600 hover:bg-blue-800 text-white font-black py-5 rounded-2xl text-center shadow-xl shadow-blue-200 transition-all transform hover:-translate-y-1 uppercase tracking-widest text-sm">Visitar Web Oficial</a>
                            <a href={`/analisis/${soft.id}/?sector=electricistas`} class="text-center text-slate-400 font-bold text-xs hover:text-blue-600 hover:underline underline-offset-4 transition-all uppercase tracking-tighter">Leer análisis técnico completo →</a>
                        </div>
                    </div>
                </div>
            </div>
        ))}
    </div>

    {/* GUÍA + BLOQUES + FAQs FINAL */}
    <section class="bg-white rounded-3xl shadow-2xl border border-slate-200 p-8 md:p-16 mb-16 text-center md:text-left">
        <h2 class="text-4xl font-black text-slate-900 mb-8 tracking-tighter underline decoration-yellow-400 decoration-4">{info.guiaTitulo}</h2>
        <p class="text-xl text-slate-600 mb-12 leading-relaxed" set:html={info.guiaTexto}></p>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-10 border-y border-slate-100 py-12 mb-16">
            {info.bloques.map(b => (
                <div class="relative group">
                    <div class="text-5xl mb-5 transform group-hover:scale-110 transition-transform">{b.icon}</div>
                    <h4 class="font-black text-slate-900 mb-3 text-lg uppercase">{b.t}</h4>
                    <p class="text-sm text-slate-500 leading-relaxed">{b.d}</p>
                </div>
            ))}
        </div>

        <h3 class="text-3xl font-black text-slate-900 mb-10 tracking-tight">Preguntas Frecuentes de Instaladores</h3>
        <div class="space-y-6">
            {info.faqs.map(faq => (
                <details class="group bg-slate-50 border border-slate-200 rounded-2xl transition-all">
                    <summary class="p-6 font-bold text-slate-800 cursor-pointer list-none flex justify-between items-center group-open:bg-blue-600 group-open:text-white rounded-2xl transition-all">
                        <span class="text-lg">{faq.q}</span>
                        <span class="transition-transform group-open:rotate-180">▼</span>
                    </summary>
                    <div class="p-8 text-slate-600 bg-white rounded-b-2xl border-t border-slate-100 text-lg leading-relaxed">{faq.a}</div>
                </details>
            ))}
        </div>
    </section>
  </main>
</Layout>