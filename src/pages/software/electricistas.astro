---
// src/pages/software/electricistas.astro
// 🚨 PÁGINA PILAR (SILO) - Especializada para Instaladores Eléctricos
import Layout from '../../layouts/Layout.astro';
import softwares from '../../softwares.json';

// 1. DATOS ESPECÍFICOS (Copywriting para Electricistas)
const info = {
  id: 'electricistas',
  nombre: 'Electricistas',
  emoji: '⚡',
  titulo: 'Software para Instaladores Electricistas y SAT',
  desc: 'Gestiona boletines, mantenimientos preventivos y actualiza el precio del cable al día. Cumple el REBT y la Ley Verifactu 2026.',
  guiaTitulo: 'Guía 2026: Digitalización para el Instalador Eléctrico',
  guiaTexto: 'El sector eléctrico tiene particularidades críticas: normativas estrictas (REBT), precios de materiales volátiles (cobre) y entornos difíciles. Necesitas automatizar las revisiones para asegurar ingresos recurrentes.',
  bloques: [
    { icon: '🔄', t: 'Mantenimientos', d: 'El secreto de la rentabilidad. Genera avisos automáticos para revisión de cuadros, extintores o fotovoltaica.' },
    { icon: '🔌', t: 'Tarifas Proveedor', d: 'Importa tarifas de proveedores (Excel) en segundos. Actualiza el precio del cobre y cable masivamente para no perder margen.' },
    { icon: '🔦', t: 'Trabajo Offline', d: 'Imprescindible para trabajar en cuartos de contadores, garajes o naves industriales sin cobertura móvil.' }
  ],
  // ⚡ FAQs ESTRATÉGICAS DE NEGOCIO PARA ELECTRICISTAS
  faqs: [
    { q: "¿Cómo puedo actualizar los precios de mis presupuestos si sube el cobre?", a: "Los programas especializados permiten importar las tarifas de tus proveedores (Excel) y aplicar un margen de beneficio automático. Así, tus presupuestos siempre reflejarán el precio actual del material sin perder margen." },
    { q: "¿Qué software me ayuda con los certificados y boletines eléctricos?", a: "Aunque la mayoría de ERPs gestionan la facturación, los recomendados permiten adjuntar los PDF de tus boletines técnicos a la ficha del cliente o integrarse con herramientas de firma digital para legalizar la instalación al momento." },
    { q: "¿Cómo puedo asegurar ingresos recurrentes en mi empresa?", a: "La clave está en los mantenimientos preventivos. El software te avisa automáticamente cuando toca revisar un cuadro eléctrico, extintores o una planta fotovoltaica, permitiéndote facturar visitas recurrentes sin esfuerzo comercial." },
    { q: "¿Es obligatorio el sistema Verifactu para un instalador autónomo?", a: "Sí. A partir de 2026, cualquier instalador debe usar un software que garantice la trazabilidad de sus facturas y genere el código QR legal. Seguir facturando con Word o Excel será motivo de sanción." }
  ]
};

// 2. FILTRADO VIP Y LÓGICA DE NEGOCIO
const vipList = ['stel-order', 'taclia'];
const electricistasSoft = softwares
  .filter(s => s.target_gremios.some(g => {
      const gremio = g.toLowerCase();
      return gremio.includes('electric') || gremio.includes('instalador') || gremio.includes('sat');
  }))
  .sort((a, b) => {
      const vipA = vipList.indexOf(a.id);
      const vipB = vipList.indexOf(b.id);
      if (vipA !== -1 && vipB !== -1) return vipA - vipB;
      if (vipA !== -1) return -1;
      if (vipB !== -1) return 1;
      return (b.legal_spain?.verifactu_ready ? 1 : 0) - (a.legal_spain?.verifactu_ready ? 1 : 0);
  });

const topTable = electricistasSoft.slice(0, 5);

// 3. SCHEMA SEO AMPLIADO CON FAQs
const schema = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": info.faqs.map(faq => ({
    "@type": "Question",
    "name": faq.q,
    "acceptedAnswer": {
      "@type": "Answer",
      "text": faq.a
    }
  }))
};
---

<Layout 
  title="Mejor Software para Electricistas 2026: Precios y Mantenimientos"
  description="¿Problemas con el precio del cable? Comparamos los 7 mejores programas para electricistas. Importación de tarifas, boletines y Verifactu."
>
  <script type="application/ld+json" set:html={JSON.stringify(schema)} />

  {/* HERO HEADER */}
  <header class="bg-blue-900 text-white pt-24 pb-20 px-6 relative overflow-hidden text-center md:text-left">
    <div class="absolute top-0 right-0 opacity-10 pointer-events-none text-[200px]">⚡</div>
    <div class="max-w-4xl mx-auto relative z-10">
      <span class="bg-yellow-500 text-yellow-900 text-xs font-bold px-3 py-1 rounded-full uppercase tracking-widest mb-4 inline-block shadow-lg">Especial Instaladores</span>
      <h1 class="text-4xl md:text-6xl font-black mb-6 leading-tight">Software para <br/><span class="text-blue-400">Electricistas e Instaladores</span></h1>
      <p class="text-xl text-blue-100 max-w-2xl mb-8 leading-relaxed">Gestiona la volatilidad de los precios del material y automatiza tus mantenimientos bajo <strong>REBT y Verifactu</strong>.</p>
    </div>
  </header>

  <main class="max-w-5xl mx-auto px-6 -mt-16 relative z-20 pb-20">

    {/* 1. CHEAT SHEET (VEREDICTO RÁPIDO) */}
    <div class="bg-white rounded-2xl shadow-xl border border-slate-200 overflow-hidden mb-16">
        <div class="bg-slate-900 p-4 text-center">
            <h2 class="text-white font-bold uppercase tracking-widest text-sm text-center">🏆 Top 3 Recomendado</h2>
        </div>
        <div class="grid md:grid-cols-3 divide-y md:divide-y-0 md:divide-x divide-slate-100">
            <div class="p-6 text-center hover:bg-blue-50 transition-colors">
                <div class="text-4xl mb-3">🥇</div>
                <h3 class="font-black text-slate-800 text-lg">STEL Order</h3>
                <span class="inline-block bg-blue-100 text-blue-800 text-xs font-bold px-2 py-1 rounded mb-3">Más Completo</span>
                <p class="text-sm text-slate-500 mb-4 h-12 line-clamp-2 italic">Ideal para catálogos de materiales y tarifas de proveedores masivas.</p>
                <a href="/analisis/stel-order?sector=electricistas" class="text-blue-600 font-bold text-sm">Ver Análisis →</a>
            </div>
            <div class="p-6 text-center hover:bg-green-50 transition-colors">
                <div class="text-4xl mb-3">🔌</div>
                <h3 class="font-black text-slate-800 text-lg">Taclia</h3>
                <span class="inline-block bg-green-100 text-green-800 text-xs font-bold px-2 py-1 rounded mb-3">Gratuito</span>
                <p class="text-sm text-slate-500 mb-4 h-12 line-clamp-2 italic">Perfecto para reparaciones rápidas y electricistas autónomos.</p>
                <a href="/analisis/taclia?sector=electricistas" class="text-green-600 font-bold text-sm">Ver Análisis →</a>
            </div>
            <div class="p-6 text-center hover:bg-purple-50 transition-colors">
                <div class="text-4xl mb-3">⚙️</div>
                <h3 class="font-black text-slate-800 text-lg">Fixner</h3>
                <span class="inline-block bg-purple-100 text-purple-800 text-xs font-bold px-2 py-1 rounded mb-3">Mantenimientos</span>
                <p class="text-sm text-slate-500 mb-4 h-12 line-clamp-2 italic">Gestión experta de activos y contratos de mantenimiento preventivo.</p>
                <a href="/analisis/fixner?sector=electricistas" class="text-purple-600 font-bold text-sm">Ver Análisis →</a>
            </div>
        </div>
    </div>

    {/* 2. TABLA COMPARATIVA */}
    <div class="bg-white rounded-2xl shadow-lg border border-slate-200 overflow-hidden mb-16">
        <div class="bg-slate-100 p-4 border-b border-slate-200 text-center md:text-left">
            <h3 class="font-bold text-slate-700 uppercase text-xs tracking-wider tracking-tighter italic">Tabla Técnica para Instaladores</h3>
        </div>
        <div class="overflow-x-auto">
            <table class="w-full text-left">
                <thead class="bg-slate-50 border-b border-slate-200 text-xs font-bold uppercase text-slate-500 text-[10px]">
                    <tr>
                        <th class="p-4">Software</th>
                        <th class="p-4">Precio</th>
                        <th class="p-4 text-center">App</th>
                        <th class="p-4"></th>
                    </tr>
                </thead>
                <tbody class="text-sm">
                    {topTable.map((s) => (
                        <tr class="border-b border-slate-100 hover:bg-blue-50/50 transition-colors">
                            <td class="p-4 font-bold text-slate-800 flex items-center gap-3 whitespace-nowrap">
                                {s.logo_url ? <img src={s.logo_url} class="w-8 h-8 object-contain" alt="" /> : "📱"} {s.name}
                            </td>
                            <td class="p-4 font-black text-blue-700">
                                {s.pricing?.start_price === 0 ? "GRATIS" : `${s.pricing.start_price}€`}
                            </td>
                            <td class="p-4 text-center">{s.tabla_tecnica?.app_movil ? '✅' : '❌'}</td>
                            <td class="p-4 text-right">
                                <a href={s.url_afiliado} target="_blank" class="bg-blue-600 text-white text-[10px] font-black px-4 py-2 rounded uppercase tracking-tighter">Ver Web</a>
                            </td>
                        </tr>
                    ))}
                </tbody>
            </table>
        </div>
    </div>

    {/* 3. LISTADO TARJETAS */}
    <div class="space-y-12 mb-20">
        {electricistasSoft.map((soft) => (
            <div class="bg-white rounded-2xl shadow-lg border border-slate-200 overflow-hidden hover:shadow-xl transition-shadow">
                <div class="p-6 md:p-10">
                    <div class="flex flex-col md:flex-row md:items-start justify-between gap-6 mb-8">
                        <div class="flex items-start gap-6">
                            <div class="w-20 h-20 bg-white rounded-xl flex items-center justify-center border border-slate-200 p-2 shrink-0">
                                {soft.logo_url ? <img src={soft.logo_url} alt={soft.name} class="w-full h-full object-contain" /> : <span class="text-3xl text-slate-300">📱</span>}
                            </div>
                            <div>
                                <h3 class="text-2xl font-black text-slate-800 underline decoration-blue-200 decoration-4 underline-offset-4">{soft.name}</h3>
                                {soft.legal_spain?.verifactu_ready && <span class="text-green-700 font-bold text-[10px] uppercase bg-green-50 px-2 py-1 rounded border border-green-200 mt-2 inline-block">✅ Verifactu</span>}
                            </div>
                        </div>
                        <div class="text-right">
                            <span class="text-3xl font-black text-slate-900">{soft.pricing?.start_price === 0 ? 'GRATIS' : `${soft.pricing?.start_price}€`}</span>
                            {soft.pricing?.has_free_trial && <div class="text-blue-600 text-xs font-bold mt-1 italic">Prueba {soft.pricing.trial_days} días</div>}
                        </div>
                    </div>
                    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                        <div class="md:col-span-2">
                            <p class="text-slate-600 leading-relaxed mb-4">{soft.opinion_experto_electricista || soft.short_description}</p>
                            <div class="flex flex-wrap gap-2">
                                {soft.features?.slice(0, 4).map(f => <span class="bg-slate-50 text-slate-700 text-[10px] font-bold px-2 py-1 rounded border border-slate-100 uppercase">{f}</span>)}
                            </div>
                        </div>
                        <div class="flex flex-col gap-3 justify-center">
                            <a href={soft.url_afiliado || "#"} target="_blank" class="bg-blue-600 hover:bg-blue-700 text-white font-black py-3 rounded-xl text-center shadow-lg transition">PROBAR WEB</a>
                            <a href={`/analisis/${soft.id}?sector=electricistas`} class="text-center text-slate-500 font-bold text-sm hover:underline italic tracking-tighter italic">Análisis completo →</a>
                        </div>
                    </div>
                </div>
            </div>
        ))}
    </div>

    {/* 4. GUÍA SEO + BLOQUES + FAQs */}
    <section class="bg-white rounded-2xl shadow-lg border border-slate-200 overflow-hidden p-8 md:p-12">
        <div class="prose prose-slate max-w-none">
            <h2 class="text-3xl font-black text-slate-800 mb-6">{info.guiaTitulo}</h2>
            <p class="text-lg leading-relaxed text-slate-600 mb-10">{info.guiaTexto}</p>
        </div>

        {/* 3 BLOQUES DE ICONOS (ESTRATÉGICOS) */}
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-12 border-y border-slate-100 py-10">
            {info.bloques.map(b => (
                <div class="text-center md:text-left">
                    <div class="text-4xl mb-4">{b.icon}</div>
                    <h4 class="font-bold text-slate-900 mb-2">{b.t}</h4>
                    <p class="text-sm text-slate-600">{b.d}</p>
                </div>
            ))}
        </div>

        <div class="prose prose-slate max-w-none">
            <h3 class="text-2xl font-black text-slate-800 mb-8">Dudas frecuentes sobre gestión eléctrica</h3>
        </div>
        <div class="space-y-4">
            {info.faqs.map(faq => (
                <details class="group bg-slate-50 border border-slate-200 rounded-xl transition-all">
                    <summary class="p-6 font-bold text-slate-800 cursor-pointer list-none flex justify-between items-center group-open:bg-blue-50 group-open:rounded-t-xl">
                        {faq.q}
                        <span class="text-blue-600 transition-transform group-open:rotate-180">▼</span>
                    </summary>
                    <div class="p-6 text-slate-600 leading-relaxed bg-white rounded-b-xl border-t border-slate-100 text-sm md:text-base">
                        {faq.a}
                    </div>
                </details>
            ))}
        </div>
    </section>

  </main>
</Layout>