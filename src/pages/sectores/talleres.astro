---
// src/pages/sectores/talleres.astro
import Header from '../../components/Header.astro';
import softwares from '../../softwares.json';
import '../../global.css';

// 1. FILTRO TALLERES
const especialistas = softwares.filter(s => 
    s.target_gremios && s.target_gremios.some(g => {
        const gremio = g.toLowerCase();
        return [
            'taller', 'mecánico', 'automoción', 'chapa', 'pintura', 
            'vehículos', 'reparaciones', 'sat', 'pymes'
        ].some(term => gremio.includes(term));
    })
);
const topTable = especialistas.slice(0, 5);

// 2. SCHEMA UPDATE
const schema = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": [
    { "@type": "Question", "name": "¿Es obligatorio el Resguardo de Depósito?", "acceptedAnswer": { "@type": "Answer", "text": "Sí. Según la normativa de Industria, todo taller debe entregar un resguardo al recibir el vehículo. El software debe generarlo automáticamente para evitar sanciones." } },
    { "@type": "Question", "name": "¿El software conecta con bases de datos de recambios?", "acceptedAnswer": { "@type": "Answer", "text": "Los mejores ERPs de taller conectan con proveedores o bases de datos como TecDoc para identificar la pieza exacta por bastidor." } }
  ]
};
---

<html lang="es">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Software de Gestión de Talleres 2025: OR, Matrículas y Verifactu</title>
    <meta name="description" content="Guía definitiva de software para talleres mecánicos. Gestión de Órdenes de Reparación, Resguardos de Depósito, Historial por Matrícula y normativa Verifactu." />
    <script type="application/ld+json" set:html={JSON.stringify(schema)} />
</head>
<body class="bg-slate-50 text-slate-900 font-sans">

    <Header />

    <header class="bg-blue-900 text-white py-16 px-6">
        <div class="max-w-4xl mx-auto">
            <h1 class="text-4xl md:text-5xl font-black mb-4 italic">
                Software para Taller Mecánico: <span class="text-blue-400">Tu taller, bajo control.</span>
            </h1>
            <p class="text-lg text-blue-100 opacity-90">
                Herramientas especializadas para automoción. Gestiona Órdenes de Reparación (OR), historial por matrícula, stock de recambios y cumple con Industria y Hacienda.
            </p>
        </div>
    </header>

    <section class="max-w-6xl mx-auto px-6 -mt-10 relative z-20 mb-16">
        <div class="bg-white rounded-2xl shadow-2xl border border-slate-200 overflow-hidden">
            <div class="bg-slate-900 p-4 flex justify-between items-center">
                <h2 class="text-white font-bold text-sm uppercase tracking-widest">Top Software Automoción 2025</h2>
                <span class="bg-green-600 text-white text-[10px] px-2 py-1 rounded font-bold animate-pulse">VERIFACTU READY</span>
            </div>
            <div class="overflow-x-auto">
                <table class="w-full text-left">
                    <thead>
                        <tr class="bg-slate-50 border-b border-slate-200 text-xs font-bold uppercase text-slate-500">
                            <th class="p-4">Software</th>
                            <th class="p-4">Especialidad</th>
                            <th class="p-4">Precio Base</th>
                            <th class="p-4 text-center">Nube</th>
                            <th class="p-4"></th>
                        </tr>
                    </thead>
                    <tbody class="text-sm">
                        {topTable.map((s) => (
                            <tr class="border-b border-slate-100 hover:bg-blue-50/50 transition-colors">
                                <td class="p-4 font-bold text-slate-800 flex items-center gap-2">
                                    {s.logo_url && <img src={s.logo_url} class="w-6 h-6 object-contain" alt="" />}
                                    {s.name}
                                </td>
                                <td class="p-4 text-slate-600 font-medium">{s.target_gremios ? s.target_gremios[0] : 'General'}</td>
                                <td class="p-4 font-black text-blue-700">{s.pricing?.start_price ? `${s.pricing.start_price}€` : '-'}</td>
                                <td class="p-4 text-center">{s.tabla_tecnica?.app_movil ? '☁️' : '-'}</td>
                                <td class="p-4"><a href={s.url_afiliado} target="_blank" class="bg-blue-600 text-white text-[10px] font-black px-3 py-2 rounded uppercase">Ver Web</a></td>
                            </tr>
                        ))}
                    </tbody>
                </table>
            </div>
        </div>
    </section>

    <main class="max-w-6xl mx-auto py-12 px-6">
        <div class="space-y-10">
            {especialistas.map((soft) => (
                <div class="bg-white rounded-2xl shadow-lg border border-slate-200 overflow-hidden hover:shadow-xl transition-all duration-300 transform hover:-translate-y-1">
                  <div class="p-6 md:p-10">
                    
                    {/* CABECERA (LOGO + TÍTULO + PRECIO) */}
                    <div class="flex flex-col md:flex-row md:items-center justify-between gap-6 mb-8 relative">
                        <div class="flex items-center gap-6">
                            <div class="w-24 h-24 bg-white rounded-xl flex items-center justify-center border border-slate-200 shadow-sm p-3 shrink-0 relative group">
                                {soft.logo_url ? (
                                    <img src={soft.logo_url} alt={`Logo ${soft.name}`} class="max-h-full max-w-full object-contain" loading="lazy" />
                                ) : (
                                    <span class="text-4xl font-black text-slate-300">{soft.name.charAt(0)}</span>
                                )}
                                {soft.legal_spain?.verifactu_ready && (
                                    <div class="absolute -top-3 -right-3 bg-green-600 text-white text-[10px] font-black px-2 py-1 rounded shadow-md border-2 border-white z-10">VERIFACTU</div>
                                )}
                            </div>
                            <div>
                                <h3 class="text-3xl font-black text-slate-800"><a href={`/analisis/${soft.id}`} class="hover:text-blue-700">{soft.name}</a></h3>
                                <div class="flex flex-col gap-1 mt-2">
                                    {soft.legal_spain?.verifactu_ready && <span class="flex items-center text-green-700 font-bold text-xs bg-green-50 px-2 py-1 rounded border border-green-200">HOMOLOGADO VERIFACTU</span>}
                                </div>
                            </div>
                        </div>
                        <div class="flex flex-col items-end">
                            <span class="text-xs font-bold text-slate-400 uppercase tracking-widest">Cuota mensual</span>
                            <span class="text-3xl font-black text-slate-900">{soft.pricing?.start_price ? `${soft.pricing.start_price}€` : 'Consultar'}</span>
                            {soft.pricing?.has_free_trial && <span class="text-blue-600 text-xs font-bold mt-1 bg-blue-50 px-2 py-0.5 rounded">Prueba Gratis {soft.pricing.trial_days} días</span>}
                        </div>
                    </div>

                    {/* GRID DE CONTENIDO (Texto Izquierda / Caja Gris Derecha) */}
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-10">
                        {/* COLUMNA IZQ: TEXTO */}
                        <div class="space-y-6">
                            <div class="prose prose-slate">
                                <h4 class="text-xl font-bold text-slate-800">¿Por qué para tu taller?</h4>
                                <p class="text-slate-600 leading-relaxed text-sm">
                                    {soft.short_description || "Gestión integral de taller, control de tiempos y facturación."}
                                </p>
                            </div>
                            <div class="flex flex-wrap gap-2">
                                {soft.features && soft.features.slice(0, 4).map(f => (
                                    <span class="bg-slate-100 text-slate-700 text-xs font-bold px-3 py-1 rounded-md border border-slate-200 uppercase tracking-tight">{f}</span>
                                ))}
                            </div>
                        </div>

                        {/* COLUMNA DER: CAJA GRIS DE ACCIÓN (BOTONES APILADOS) */}
                        <div class="flex flex-col justify-center bg-slate-50 p-6 rounded-2xl border border-slate-100">
                            <h4 class="font-bold text-slate-800 mb-4 text-center italic text-sm">
                                "Controla matrículas, ORs y facturas en un solo lugar"
                            </h4>
                            <div class="space-y-3">
                                <a href={soft.url_afiliado || "#"} target="_blank" class="block w-full text-center bg-blue-600 hover:bg-blue-700 text-white font-black py-4 rounded-xl text-lg shadow-xl shadow-blue-200 transition-transform active:scale-95">
                                    VER WEB DE {soft.name.toUpperCase()}
                                </a>
                                <a href={`/analisis/${soft.id}`} class="block w-full text-center bg-white border-2 border-slate-200 text-slate-700 font-bold py-3 rounded-xl hover:bg-slate-50 transition-colors text-sm">
                                    Leer análisis detallado
                                </a>
                            </div>
                            <p class="text-[10px] text-center text-slate-400 mt-4 uppercase font-bold tracking-widest">Enlace oficial verificado · 2025</p>
                        </div>
                    </div>

                  </div>
                </div>
            ))}
        </div>

        {/* --- GUÍA SEO EXPANDIDA (Contenido específico TALLERES) --- */}
        <div class="mt-20 bg-white rounded-2xl shadow-lg border border-slate-200 overflow-hidden p-8 md:p-12">
            
            <div class="prose prose-slate max-w-none">
                <h2 class="text-3xl font-black text-slate-800 mb-6">Guía 2025: Cómo digitalizar tu Taller Mecánico sin morir en el intento</h2>
                <p class="text-lg leading-relaxed text-slate-600">
                    Gestionar un taller en 2025 es una tarea de malabarismo. Ya no basta con ser un buen mecánico; tienes que ser gestor, contable y experto legal. 
                    Entre la normativa de <strong>Industria</strong> (Resguardos, Placas), la de <strong>Consumo</strong> (Garantías) y ahora la de <strong>Hacienda</strong> (Verifactu), el Excel se ha convertido en una herramienta peligrosa.
                </p>
                <p class="text-lg leading-relaxed text-slate-600">
                    Elegir el software adecuado (DMS o ERP de Taller) es la única forma de blindar tu negocio ante multas y, sobre todo, de dejar de perder dinero en piezas no cobradas o tiempos muertos.
                </p>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-3 gap-8 not-prose my-10 border-y border-slate-100 py-10">
                <div class="p-6 bg-blue-50 rounded-2xl border border-blue-100">
                    <div class="text-4xl mb-4">🚗</div>
                    <h4 class="font-bold text-blue-900 mb-3 text-lg">1. Trazabilidad por Matrícula</h4>
                    <p class="text-sm text-blue-800 leading-relaxed">
                        El núcleo de tu negocio es el vehículo. El software debe permitirte introducir la matrícula y ver al instante todo el <strong>historial clínico</strong> del coche: últimas revisiones, aceites usados y reparaciones pendientes.
                    </p>
                </div>
                <div class="p-6 bg-green-50 rounded-2xl border border-green-100">
                    <div class="text-4xl mb-4">📋</div>
                    <h4 class="font-bold text-green-900 mb-3 text-lg">2. Legalidad: OR y Resguardos</h4>
                    <p class="text-sm text-green-800 leading-relaxed">
                        Es obligatorio entregar un <strong>Resguardo de Depósito</strong> al recibir el coche. El software debe generarlo con un clic, permitiendo firmar en tablet, y convertirlo luego en una <strong>Orden de Reparación (OR)</strong> y finalmente en Factura.
                    </p>
                </div>
                <div class="p-6 bg-purple-50 rounded-2xl border border-purple-100">
                    <div class="text-4xl mb-4">🔧</div>
                    <h4 class="font-bold text-purple-900 mb-3 text-lg">3. Stock y Margen de Piezas</h4>
                    <p class="text-sm text-purple-800 leading-relaxed">
                        El beneficio del taller está en la mano de obra y en el margen del recambio. Un buen ERP descuenta el filtro de aceite del stock en cuanto lo añades a la OR, y te avisa si estás vendiendo por debajo del margen deseado.
                    </p>
                </div>
            </div>

            <div class="prose prose-slate max-w-none">
                <h3 class="text-2xl font-bold text-slate-800">Preguntas Frecuentes del Jefe de Taller</h3>
                
                <details class="group bg-slate-50 p-5 rounded-xl cursor-pointer mb-4 border border-slate-100">
                    <summary class="font-bold text-slate-800 flex justify-between items-center list-none text-lg">
                        ¿Qué pasa con la nueva Ley Antifraude en talleres?
                        <span class="text-blue-600 group-open:rotate-180 transition-transform">▼</span>
                    </summary>
                    <p class="text-slate-600 mt-3 leading-relaxed">
                        Los talleres son un sector vigilado. La Ley "Verifactu" prohíbe el software que permita ocultar tickets o modificar facturas ya emitidas (la famosa "Caja B"). Todos los programas que recomendamos en ObraDigital generan una huella digital inalterable.
                    </p>
                </details>

                <details class="group bg-slate-50 p-5 rounded-xl cursor-pointer mb-4 border border-slate-100">
                    <summary class="font-bold text-slate-800 flex justify-between items-center list-none text-lg">
                        ¿Puedo conectar con proveedores de recambios?
                        <span class="text-blue-600 group-open:rotate-180 transition-transform">▼</span>
                    </summary>
                    <p class="text-slate-600 mt-3 leading-relaxed">
                        Depende del programa. Los softwares específicos de automoción suelen tener integraciones con bases de datos como <strong>TecDoc</strong> o plataformas de pedidos.
                    </p>
                </details>

                <p className="text-sm italic text-slate-500 mt-8">
                    * Nota: Esta comparativa se actualiza mensualmente revisando las nuevas normativas de la DGT, Industria y Hacienda aplicables a talleres mecánicos y de carrocería en España.
                </p>
            </div>
        </div>

    </main>
</body>
</html>